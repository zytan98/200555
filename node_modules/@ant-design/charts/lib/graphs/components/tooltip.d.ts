import { Graph, TooltipCfg, NodeCfg } from '../interface';
export interface ITooltip {
    tooltipCfg: TooltipCfg;
    graph: Graph;
    container: HTMLDivElement | null;
    nodeCfg: NodeCfg;
}
export declare const createTooltip: ({ graph, container, tooltipCfg, nodeCfg }: ITooltip) => void;
